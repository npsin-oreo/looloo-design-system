{
  "$description": "Captured 1:1 from components/ui/spinner/spinner.tsx — a Loader2 icon at size-4 with animate-spin. The 1s linear rotation is Tailwind's built-in animate-spin, not a brand motion token; only the size is ours.",
  "spinner": {
    "size": {
      "$type": "dimension",
      "$value": "{size.16}",
      "$description": "size-4 — callers override for larger spinners.",
      "$extensions": {
        "looloo.tailwind": "size-4",
        "looloo.legacy": "1rem"
      }
    },
    "duration": {
      "$type": "duration",
      "$value": "1000ms",
      "$description": "Tailwind's animate-spin (1s linear infinite). NOT motion.duration.base (200ms) — a loop, not a transition.",
      "$extensions": {
        "looloo.tailwind": "animate-spin"
      }
    }
  }
}
