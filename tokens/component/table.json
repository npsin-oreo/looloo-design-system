{
  "$description": "DRAFT — captured from components/ui/table.tsx. Not consumed at runtime yet (Phase 4). rowHeight is the density-mode hook (compact/comfortable).",
  "table": {
    "headerHeight": {
      "$type": "dimension",
      "$value": "{size.40}",
      "$extensions": {
        "looloo.tailwind": "h-10"
      },
      "$description": "Table header is a row, not a touch target — pinned to 40px when size.control.xl moved to 44."
    },
    "cellPadding": {
      "$type": "dimension",
      "$value": "{space.2}",
      "$extensions": {
        "looloo.tailwind": "p-2"
      }
    },
    "headerPaddingX": {
      "$type": "dimension",
      "$value": "{space.2}",
      "$extensions": {
        "looloo.tailwind": "px-2"
      }
    },
    "fontSize": {
      "$type": "dimension",
      "$value": "{font.size.sm}",
      "$extensions": {
        "looloo.tailwind": "text-sm"
      }
    },
    "headerFontWeight": {
      "$type": "fontWeight",
      "$value": "{font.weight.medium}",
      "$description": "The header is the only row in the table that is not data — weight is what says so.",
      "$extensions": {
        "looloo.tailwind": "font-medium"
      }
    },
    "rowBorderWidth": {
      "$type": "dimension",
      "$value": "{border.width.default}",
      "$description": "Every row carries its own bottom border — the LAST one drops it, so the table does not end in a line that looks like a broken row.",
      "$extensions": {
        "looloo.tailwind": "border-b · [&_tr:last-child]:border-0"
      }
    },
    "caption": {
      "marginTop": {
        "$type": "dimension",
        "$value": "{space.4}",
        "$extensions": {
          "looloo.tailwind": "mt-4"
        }
      },
      "fontSize": {
        "$type": "dimension",
        "$value": "{font.size.sm}",
        "$extensions": {
          "looloo.tailwind": "text-sm"
        }
      }
    },
    "sortIconSize": {
      "$type": "dimension",
      "$value": "{size.16}",
      "$description": "The sort affordance. Present only on columns that CAN be sorted — a sort icon on every column teaches people it means nothing.",
      "$extensions": {
        "looloo.tailwind": "size-4"
      }
    },
    "cellMinHeight": {
      "$type": "dimension",
      "$value": "{size.48}",
      "$description": "Every cell is at least 48px tall, whatever it holds. Without this the row height follows its tallest cell — a text cell is 32, a cell with an input is 44, one with a two-line value is 52 — and the rows come out ragged, so the eye has to re-find every column on the way down. 48 also clears the touch floor for the controls that live in cells.",
      "$extensions": {
        "looloo.tailwind": "min-h-(--table-cell-min-height)"
      }
    }
  }
}
