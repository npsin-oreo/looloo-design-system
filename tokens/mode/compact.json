{
  "$description": "PROPOSED (not consumed yet) — density mode for dense admin/productivity screens. Overrides step every control size one notch DOWN from the measured radix-nova baseline (md h-8 → h-7). Wire-up target: Phase 4+ after component tokens drive real CSS vars.",
  "mode": {
    "compact": {
      "size": {
        "control": {
          "sm": { "$value": "1.5rem" },
          "md": { "$value": "1.75rem" },
          "lg": { "$value": "2rem" },
          "xl": { "$value": "2.25rem" }
        }
      },
      "table": {
        "headerHeight": { "$value": "2.25rem" },
        "cellPadding": { "$value": "0.375rem" }
      }
    }
  }
}
