{
  "$description": "Canonical radius steps from the Figma kit. Generated; do not hand-edit.",
  "radius": {
    "xs": {
      "$type": "dimension",
      "$value": "0.125rem",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{2}"
      }
    },
    "sm": {
      "$type": "dimension",
      "$value": "0.25rem",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{4}"
      }
    },
    "md": {
      "$type": "dimension",
      "$value": "0.375rem",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{6}"
      }
    },
    "lg": {
      "$type": "dimension",
      "$value": "0.5rem",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{8}"
      }
    },
    "xl": {
      "$type": "dimension",
      "$value": "0.75rem",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{12}"
      }
    },
    "2xl": {
      "$type": "dimension",
      "$value": "1rem",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{16}"
      }
    },
    "3xl": {
      "$type": "dimension",
      "$value": "1.5rem",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{24}"
      }
    },
    "4xl": {
      "$type": "dimension",
      "$value": "2rem",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{32}"
      }
    },
    "none": {
      "$type": "dimension",
      "$value": "0",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{0}"
      }
    },
    "full": {
      "$type": "dimension",
      "$value": "9999px",
      "$description": "Static Figma-kit step. NOTE: the runtime radius scale is currently calc(var(--radius) * n) in app/globals.css; that stays authoritative until Phase 2.",
      "$extensions": {
        "looloo.legacy": "{9999}"
      }
    }
  }
}
