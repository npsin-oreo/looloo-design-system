{
  "$description": "Surface roles — aliases into the semantic color layer (tokens/semantic/color.json). Hand-owned (NOT regenerated by tokens:migrate). Not consumed at runtime yet; the CSS build lands in Phase 2.",
  "surface": {
    "background": {
      "$type": "color",
      "$value": "{color.background}",
      "$description": "Default app background."
    },
    "card": {
      "$type": "color",
      "$value": "{color.card}",
      "$description": "Card and elevated container surface."
    },
    "popover": {
      "$type": "color",
      "$value": "{color.popover}",
      "$description": "Floating surfaces: popover, dropdown, select content, tooltip panel."
    },
    "muted": {
      "$type": "color",
      "$value": "{color.muted}",
      "$description": "Subdued fills: hover states, secondary areas, skeleton base."
    },
    "sidebar": {
      "$type": "color",
      "$value": "{color.sidebar}",
      "$description": "App sidebar surface."
    },
    "overlay": {
      "$type": "color",
      "$value": "oklch(0 0 0 / 0.1)",
      "$description": "Modal scrim (alert-dialog/drawer/sheet): black @ 10% paired with supports-backdrop-filter:backdrop-blur-xs. DESIGN DECISION 2026-07-08: keep this (the rendered behavior); the Figma kit's 30% `background-color` extra (color.background-color) is superseded for scrim use — it remains only as a legacy kit value. Dialog itself renders no scrim."
    }
  }
}
