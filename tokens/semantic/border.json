{
  "$description": "Border roles — aliases into the semantic color layer. Hand-owned (NOT regenerated by tokens:migrate). Not consumed at runtime yet.",
  "border": {
    "default": {
      "$type": "color",
      "$value": "{color.border}",
      "$description": "Default border (globals.css applies border-border globally)."
    },
    "input": {
      "$type": "color",
      "$value": "{color.input}",
      "$description": "Form control borders (input, select trigger, textarea)."
    },
    "focus": {
      "$type": "color",
      "$value": "{color.ring}",
      "$description": "Focus-visible border color (focus-visible:border-ring)."
    },
    "sidebar": {
      "$type": "color",
      "$value": "{color.sidebar-border}"
    }
  }
}
